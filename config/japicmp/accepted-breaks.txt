# API breaks accepted by the API compatibility check (./gradlew japicmp, which ./gradlew build runs).
#
# The check compares each published artifact with its newest release on Maven Central that
# is no higher than the version in the root build.gradle, and fails on any binary- or source-incompatible change to a
# public or protected member. A break that is intended, for a major release, goes here with its reason. One file serves
# every artifact, because each element is fully qualified. japicmp can't see an interface becoming sealed. The packages in a project's apiCheck block are never
# compared, so they need no lines: the internal package io.github.brantunger.unruly.core, and, for unruly-engine, the
# packages that moved to unruly-engine-core. Each line is:
#
#   <major version> | <kind> | <element> | <justification>
#
# - major version: the major release the break ships in, such as 2. A line applies while the baseline is from an
#   earlier major version, so a 1.x hotfix released in the meantime doesn't turn it off. Once that major version is
#   released and becomes the baseline, the check warns that the line no longer applies, and it can be deleted.
# - kind: package, class, method or field. Every change to that element is accepted, not only the reported one.
# - element: as japicmp matches it. Parameter types are fully qualified and without generics, and a constructor
#   is named after its class:
#     io.github.brantunger.unruly.mvel
#     io.github.brantunger.unruly.api.RulesEngineBuilder
#     io.github.brantunger.unruly.api.RulesEngine#run(io.github.brantunger.unruly.api.FactStore)
#     io.github.brantunger.unruly.api.Rule#Rule(java.lang.String,java.lang.String,java.lang.String,java.lang.Integer,java.lang.String)
#     io.github.brantunger.unruly.mvel.MvelExpressionLanguage#LANGUAGE_NAME
# - justification: why users can live with the break. Required.
#
# A pull request that adds a line must be titled with a ! (for example feat!:), so it releases a new major version.

2 | class | io.github.brantunger.unruly.api.language.CompiledCondition | copy() is removed: every run shares a compiled condition, and evaluate(context, session) gets the run's Session instead (#280). A language keeps its per-run state in a session from ExpressionCompiler.newSession(), or returns Session.none(); the migration guide shows each change.
2 | class | io.github.brantunger.unruly.api.language.CompiledAction | copy() is removed: every run shares a compiled action, and execute(context, session) gets the run's Session instead (#280). A language keeps its per-run state in a session from ExpressionCompiler.newSession(), or returns Session.none(); the migration guide shows each change.
2 | class | io.github.brantunger.unruly.api.language.ExpressionCompiler | newSession() is abstract, so every language decides whether its expressions keep state while they run (#280, replacing #240's abstract copy()). A language without such state returns Session.none().
2 | class | io.github.brantunger.unruly.api.language.CompileContext | warn(Expression, Issue) is new, so languages can report warnings that don't fail loading (#281). CompileContext is sealed to the engine's record in 2.0 (#212), so no language can implement it.
2 | class | io.github.brantunger.unruly.api.Rule | Rule is an immutable final class (#211): the no-arg and positional constructors, the setters and canEqual, deprecated since 1.4.0 and 1.8.0, are removed, and getRuleName() is never null (#284). Rules are created with Rule.builder(), copied with toBuilder(), and read from JSON through the documented Jackson mix-ins; the migration guide shows each change.
2 | class | io.github.brantunger.unruly.api.Rule$RuleBuilder | Rule.RuleBuilder is final (#211), and build() rejects a rule without a name, a condition or an action (#284). Code calls its methods and never needed to subclass it.
2 | class | io.github.brantunger.unruly.api.FactStore | FactStore no longer extends Map, and has a new abstract asMap(), a read-only view of the facts by name (#214). Code reads a store through getValue, setValue, put and asMap(); FactMap still implements Map, so a FactMap variable keeps every Map method. The migration guide shows each change.
2 | class | io.github.brantunger.unruly.api.FactMap | Excluded only because it implements FactStore: japicmp can't load a class whose excluded interface is missing. Its one change is the new asMap(), and it still implements Map, so code that uses a FactMap compiles unchanged (#214).
2 | class | io.github.brantunger.unruly.api.FactReference | setName and setValue, deprecated since 1.8.0, are removed, so a stored fact can't change or be renamed away from its key (#214). Code creates a new Fact, or calls FactStore.setValue.
2 | class | io.github.brantunger.unruly.api.Fact | Fact is an immutable final class with a required name (#214): its setters, deprecated since 1.8.0, and the Fact(value) constructor, which named a fact after value.toString(), are removed. Code uses new Fact<>(name, value).
2 | method | io.github.brantunger.unruly.api.RulesEngine#run(io.github.brantunger.unruly.api.FactStore) | run takes a FactStore<?> instead of a FactStore<Object>, so a FactMap<Applicant> is accepted (#214). Callers compile unchanged; a class that implements RulesEngine declares run(FactStore<?> facts) instead.
