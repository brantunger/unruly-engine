# Rule bug reports

Rule bug reports should identify the expression language, the artifact or
version under test, the smallest reproducing rule, and whether the engine is
used in stateless or stateful mode. This information lets maintainers reproduce
loader and compiler failures without guessing the runtime context.
